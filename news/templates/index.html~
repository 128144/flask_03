<h1>this is index.html </h1>
<ul>
    {% for i in file %}
    <li>
     <a href=http://127.0.0.1:3000/files/{{i.id}}> title: {{i.title}}</a>
         {% for tag in file.tags %}
             <spa>{{tag}}</span>
         {% endfor %}
     <p> {{i.id}}</p>
    </li>
    {% endfor %}
</ul>
